<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vv.finance.investment.bg.stock.info.mapper.StockEconomyMapper">

    <!--    <insert id="batchSaveOrUpdate" parameterType="list">-->
    <!--        REPLACE into t_stock_economy (code,stockTime,buy_econom,sale_econom)-->
    <!--        values-->
    <!--        <foreach collection="list" item="item"  separator=",">-->
    <!--            (#{item.code},#{item.stockTime},#{item.buy_econom},-->
    <!--            #{item.sale_econom})-->
    <!--        </foreach>-->
    <!--    </insert>-->
</mapper>
